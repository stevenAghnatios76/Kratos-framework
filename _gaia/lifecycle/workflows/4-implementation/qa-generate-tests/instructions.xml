<workflow name="qa-generate-tests">
<critical>
  <mandate>Story must be in review status before QA testing</mandate>
  <mandate>Do NOT write executable test files to the source tree (tests/, spec/, __tests__/, e2e/). Document test cases in the QA report only, written to {test_artifacts}/.</mandate>
  <mandate>Output must include machine-readable PASSED or FAILED verdict</mandate>
</critical>
<step n="1" title="Load Story and Code">
  <action>Read story acceptance criteria</action>
  <action>Read implemented code files</action>
</step>
<step n="2" title="Status Gate">
  <action>Verify story is in 'review' status</action>
  <check if="status != review">HALT: Story must be in review status before QA testing</check>
</step>
<step n="3" title="API Test Case Documentation">
  <action>Document API/integration test cases for endpoints — do NOT write executable test files</action>
  <action>For each test case: describe endpoint, method, input, expected response, and edge cases</action>
  <action>Cover happy path, error cases, boundary conditions, and authorization scenarios</action>
</step>
<step n="4" title="E2E Test Case Documentation">
  <action>Document end-to-end test scenarios from user journeys — do NOT write executable test files</action>
  <action>For each scenario: describe preconditions, user actions, assertions, and cleanup</action>
</step>
<step n="5" title="Test Execution">
  <action>If executable tests already exist in the source tree for this story: run them and report results</action>
  <action>If no existing tests: run the application and manually verify key acceptance criteria through API calls or UI inspection</action>
  <action>Do NOT create new test files in the source tree — test file creation is the responsibility of /gaia-test-automate</action>
</step>
<step n="6" title="Verdict">
  <action>If ALL tests pass: verdict is PASSED</action>
  <action>If ANY test fails: verdict is FAILED — list failing tests with reasons</action>
</step>
<step n="7" title="Generate Output">
  <template-output file="{test_artifacts}/{story_key}-qa-tests.md">
    Generate QA test report with test files created, execution results, and machine-readable verdict (PASSED/FAILED).
  </template-output>
</step>
<step n="8" title="Update Review Gate and Transition">
  <action>Read story file, locate Review Gate table</action>
  <action>If PASSED: update QA Tests row status to PASSED, link report file</action>
  <action>If FAILED: update QA Tests row status to FAILED, link report file. Do NOT change story status — the review-gate-check protocol or run-all-reviews orchestrator handles status transitions.</action>
  <template-output file="{implementation_artifacts}/{story_key}-*.md">
    Update story file Review Gate table with QA test result and report link.
  </template-output>
  <invoke-protocol name="review-gate-check" file="{project-root}/_gaia/core/protocols/review-gate-check.xml" />
</step>
</workflow>
