<workflow name="epic-status">
<critical>
  <mandate>Read ONLY frontmatter from story files — never load full story content (token budget protection)</mandate>
  <mandate>Stories with no .md file are counted as backlog status</mandate>
  <mandate>Cross-sprint aggregation — do NOT filter by current sprint</mandate>
</critical>
<step n="1" title="Load Epics Manifest">
  <action>Read {planning_artifacts}/epics-and-stories.md</action>
  <action>Parse all epics: extract epic number, title, goal, done criteria</action>
  <action>Extract all story keys associated with each epic</action>
  <action>Build epic-to-stories mapping</action>
</step>
<step n="2" title="Scan Story Files">
  <action>List all .md files in {implementation_artifacts}/</action>
  <action>For each file, read ONLY the YAML frontmatter (stop at closing ---)</action>
  <action>Extract: story key, status, sprint_id (if present)</action>
  <action>Skip non-story files (e.g., sprint-status.yaml, epic-status.md)</action>
</step>
<step n="3" title="Aggregate Status">
  <action>Join scanned story statuses to epic-story mapping from step 1</action>
  <action>Stories in manifest but with no file: mark as backlog</action>
  <action>Per epic: count done, in-progress, blocked, review, ready-for-dev, validating, backlog</action>
  <action>Per epic: compute completion percentage (done / total * 100)</action>
  <action>Compute overall totals across all epics</action>
  <action>Collect any story files found that are NOT in any epic (untracked)</action>
  <action>Mark epics where all stories are done as [COMPLETE]</action>
</step>
<step n="4" title="Dependency Analysis">
  <action>For each story scanned in Step 2: check frontmatter for `dependencies` or `blocked_by` fields (list of story keys this story depends on)</action>
  <action>Build cross-epic dependency graph: for each dependency, check if the depended-on story belongs to a different epic. Record cross-epic edges.</action>
  <action>Identify blocked stories: stories whose dependencies include stories that are NOT yet `done`. List each blocked story with its blocker(s) and blocker status.</action>
  <action>Calculate critical path: find the longest chain of sequential dependent stories across all epics. Report the chain and its total length.</action>
  <action>If no story files contain dependency metadata: note "No dependency data available — add `dependencies` field to story frontmatter for cross-epic tracking." Skip blocked story and critical path calculations.</action>
</step>
<step n="5" title="Generate Dashboard">
  <action>Load template from {project-root}/_gaia/lifecycle/templates/epic-status-template.md</action>
  <action>Fill summary table with per-epic row: Total | Done | In Progress | Review | Blocked | Backlog | Completion %. Backlog = stories not yet started (no file, or status is backlog/ready-for-dev/validating). Do NOT use "Remaining" (total - done) as it overlaps with In Progress.</action>
  <action>Fill per-epic detail sections with ASCII progress bar and story-level status table</action>
  <action>ASCII progress bar: use filled blocks (█) for done, light blocks (░) for remaining, 20 chars wide</action>
  <action>Fill untracked stories section</action>
  <action>Fill dependency analysis section: cross-epic dependencies table (source story → target story → target epic → target status), blocked stories list with blockers, and critical path summary. If no dependency data, include the "no data" note from Step 4.</action>
  <template-output file="{implementation_artifacts}/epic-status.md">
    Write the completed epic status dashboard using the template.
    Include all epics, all stories, completion percentages, progress bars, and dependency analysis.
  </template-output>
</step>
</workflow>
